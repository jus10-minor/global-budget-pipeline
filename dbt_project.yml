name: "global_budget_analysis"
version: "1.0.0"
config-version: 2

profile: "global_budget_analysis"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  global_budget_analysis:
    staging:
      +materialized: view
    marts:
      +materialized: table
